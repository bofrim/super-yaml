# TOODs

- add module manifests
- add constraint error messages
- add multi-line strings
- add code gen support for built-in constraints, or remove built-in constraints
- Include constraint text as docs for generated code

- formalize the implicit structures of of the language. We should have some core meta types that are always avaiable
